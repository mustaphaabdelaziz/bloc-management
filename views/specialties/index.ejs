<% layout('layouts/boilerplate') %>
<!-- views/specialties/index.ejs -->
<div class="material-page-header">
    <div class="container">
        <div class="d-flex justify-content-between align-items-center">
            <div>
                <h1 class="page-title mb-0">Gestion des Spécialités</h1>
                <p class="page-subtitle">Configuration et gestion des spécialités médicales</p>
            </div>
            <div class="d-flex gap-2">
                <a href="/specialties/new" class="action-button">
                    <i class="bi bi-plus-circle"></i>Nouvelle Spécialité
                </a>
                <div class="dropdown">
                    <button class="action-button dropdown-toggle" type="button" id="excelDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-file-earmark-spreadsheet"></i>Import Excel
                    </button>
                    <ul class="dropdown-menu" aria-labelledby="excelDropdown">
                        <li><a class="dropdown-item" href="/specialties/template" target="_blank">
                            <i class="bi bi-download me-2"></i>Télécharger Modèle
                        </a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="#" onclick="showImportModal()">
                            <i class="bi bi-upload me-2"></i>Importer Fichier
                        </a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="container">
    <!-- Filtres -->
    <div class="material-filters">
        <h5 class="filter-title">
            <i class="bi bi-funnel"></i>Filtres et Recherche
        </h5>
        <form method="GET" class="row g-3">
            <div class="col-md-8">
                <label class="form-label">Recherche</label>
                <input type="text" name="search" class="form-control" placeholder="Nom, code ou description de la spécialité..." value="<%= typeof filters !== 'undefined' && filters.search ? filters.search : '' %>">
            </div>
            <div class="col-md-4">
                <label class="form-label">&nbsp;</label>
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-outline-primary">
                        <i class="bi bi-search"></i>
                    </button>
                    <a href="/specialties" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-clockwise"></i>
                    </a>
                </div>
            </div>
        </form>
    </div>

    <!-- Liste des spécialités -->
    <div class="material-card">
        <div class="card-header">
            <h5 class="card-title">
                <i class="bi bi-list-ul"></i>Liste des Spécialités
                <span class="badge bg-light text-primary ms-2"><%= specialties.length %></span>
            </h5>
        </div>
        <div class="card-body">
            <% if (specialties.length > 0) { %>
                <div class="material-table">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Code</th>
                                <th>Spécialité</th>
                                <th>Description</th>
                                <th>Créé par</th>
                                <th>Modifié par</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% specialties.forEach(specialty => { %>
                                <tr>
                                    <td>
                                        <span class="badge bg-light text-dark fw-bold"><%= specialty.code %></span>
                                    </td>
                                    <td>
                                        <div class="material-name">
                                            <div class="material-avatar">
                                                <i class="bi bi-award"></i>
                                            </div>
                                            <div class="material-details">
                                                <h6><%= specialty.name %></h6>
                                            </div>
                                        </div>
                                    </td>
                                    <td>
                                        <% if (specialty.description) { %>
                                            <%= specialty.description.length > 50 ? specialty.description.substring(0, 50) + '...' : specialty.description %>
                                        <% } else { %>
                                            <span class="text-muted">Non spécifiée</span>
                                        <% } %>
                                    </td>
                                    <td>
                                        <small>
                                            <% if (specialty.createdBy) { %>
                                                <%= specialty.createdBy.firstname %> <%= specialty.createdBy.lastname %>
                                                <br>
                                                <span class="text-muted"><%= typeof moment !== 'undefined' ? moment(specialty.createdAt).format('DD/MM/YYYY HH:mm') : '' %></span>
                                            <% } else { %>
                                                <span class="text-muted">-</span>
                                            <% } %>
                                        </small>
                                    </td>
                                    <td>
                                        <small>
                                            <% if (specialty.updatedBy) { %>
                                                <%= specialty.updatedBy.firstname %> <%= specialty.updatedBy.lastname %>
                                                <br>
                                                <span class="text-muted"><%= typeof moment !== 'undefined' ? moment(specialty.updatedAt).format('DD/MM/YYYY HH:mm') : '' %></span>
                                            <% } else { %>
                                                <span class="text-muted">-</span>
                                            <% } %>
                                        </small>
                                    </td>
                                    <td>
                                        <div class="material-actions">
                                            <a href="/specialties/<%= specialty._id %>/edit" class="btn btn-outline-warning btn-sm" title="Modifier">
                                                <i class="bi bi-pencil"></i>
                                            </a>
                                            <button type="button" class="btn btn-outline-danger btn-sm" onclick="deleteSpecialty('<%= specialty._id %>')" title="Supprimer">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <div class="material-empty-state">
                    <div class="empty-icon">
                        <i class="bi bi-award"></i>
                    </div>
                    <h5 class="empty-title">Aucune spécialité trouvée</h5>
                    <p class="empty-description">Commencez par ajouter des spécialités médicales</p>
                    <a href="/specialties/new" class="btn btn-primary">
                        <i class="bi bi-plus-circle"></i>Ajouter la première spécialité
                    </a>
                </div>
            <% } %>
        </div>
    </div>
</div>

<script>
function deleteSpecialty(id) {
    if (confirm('Êtes-vous sûr de vouloir supprimer cette spécialité ?')) {
        const form = document.createElement('form');
        form.method = 'POST';
        form.action = '/specialties/' + id + '?_method=DELETE';
        document.body.appendChild(form);
        form.submit();
    }
}

function showImportModal() {
    const modal = new bootstrap.Modal(document.getElementById('importExcelModal'));
    modal.show();
}
</script>

<!-- Modal pour importer des spécialités -->
<div class="modal fade material-modal" id="importExcelModal" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header bg-success">
                <h5 class="modal-title">
                    <i class="bi bi-upload"></i>Importer Spécialités depuis Excel
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <form id="importForm" method="POST" action="/specialties/import" enctype="multipart/form-data">
                <div class="modal-body">
                    <!-- Download Template Button -->
                    <div class="alert alert-info d-flex justify-content-between align-items-center" role="alert">
                        <div>
                            <i class="bi bi-info-circle me-2"></i>
                            <strong>Besoin d'un modèle ?</strong> Téléchargez le fichier Excel pré-formaté avec des exemples.
                        </div>
                        <div>
                            <button type="button" id="downloadTemplate" class="btn btn-sm btn-outline-primary me-2">
                                <i class="bi bi-download me-1"></i>Télécharger (Navigateur)
                            </button>
                            <a href="/specialties/template" class="btn btn-sm btn-outline-secondary" download>
                                <i class="bi bi-cloud-download me-1"></i>Télécharger (Serveur)
                            </a>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="excelFileInput" class="form-label required-field">Fichier Excel</label>
                        <div class="input-group">
                            <input type="file" class="form-control" id="excelFileInput" name="excelFile" accept=".xlsx,.xls" required>
                            <span class="input-group-text">
                                <i class="bi bi-file-earmark-spreadsheet"></i>
                            </span>
                        </div>
                        <small class="form-text text-muted d-block mt-2">
                            Formats acceptés: .xlsx, .xls | Taille maximale: 5 MB
                        </small>
                    </div>

                    <div class="alert alert-warning" role="alert">
                        <strong><i class="bi bi-exclamation-triangle me-2"></i>Format du fichier Excel:</strong>
                        <div class="row mt-3">
                            <div class="col-md-6">
                                <strong>Colonnes requises:</strong>
                                <ul class="mb-0 mt-1 small">
                                    <li><strong>Code</strong> - Code unique de la spécialité</li>
                                    <li><strong>Nom</strong> - Nom complet de la spécialité</li>
                                </ul>
                            </div>
                            <div class="col-md-6">
                                <strong>Colonnes optionnelles:</strong>
                                <ul class="mb-0 mt-1 small">
                                    <li>Description - Brève description (jusqu'à 500 caractères)</li>
                                </ul>
                            </div>
                        </div>
                    </div>

                    <div class="alert alert-light border" role="alert">
                        <small class="text-muted">
                            <i class="bi bi-lightbulb me-1"></i>
                            <strong>Important:</strong> Le code et le nom doivent être uniques dans le système.
                            Chaque ligne vide sera ignorée.
                        </small>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="bi bi-x-circle me-1"></i>Annuler
                    </button>
                    <button type="submit" class="btn btn-success" id="importBtn">
                        <i class="bi bi-upload me-1"></i>Importer
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
<script src="/js/specialty.js"></script>
